name: Issue Handler
on:
  issue_comment:
    types: [created]
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Call AI
        uses: anthropics/claude-code-action@v1
        with:
          prompt: "Summarise this: ${{ github.event.comment.body }}"