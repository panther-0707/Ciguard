name: Issue Handler
on:
  pull_request_target:
    types: [opened]
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Call AI
        uses: anthropics/claude-code-action@v1
        with:
          prompt: "Review this: $(cat README.md)"