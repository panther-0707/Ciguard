name: Issue Handler
on:
  issues:
    types: [opened]
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Print issue
        run: echo "${{ github.event.issue.body }}"
      - name: Call AI
        uses: anthropics/claude-code-action@v1